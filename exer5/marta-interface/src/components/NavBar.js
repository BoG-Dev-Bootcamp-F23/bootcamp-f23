import { useState } from 'react';

export default function NavBar(props) {
    const { currColor, data } = props;

    const [lineColor, setLineColor] = useState(currColor);

    return (
        <div>
            <p>dfkaljsfd</p>
        </div>
    );
};